# Copy to .env.local (git-ignored). Public RevenueCat SDK keys for the shared app; without
# them the hosted paywall shows "plans unavailable" and BYOK keeps working.
EXPO_PUBLIC_REVENUECAT_IOS_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=
